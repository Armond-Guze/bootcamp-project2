<!-- commentCard partial -->

  <div class="card mb-3">
    <div class="card-header">
      <h5 class="card-title">{{user.username}}</h5>
      <h6 class="card-subtitle mb-2 text-muted">{{format_date createdAt}}</h6>
    </div>
    <div class="card-body">
      <p class="card-text">{{comment_body}}</p>
    </div>
  </div>
<script src="./js/comment.js"></script>